import React from "react";

export interface TermInputFieldProps {

}

const TermInputField: (props: TermInputFieldProps) => JSX.Element = () => (
    <div>
    </div>
);

export default TermInputField;
