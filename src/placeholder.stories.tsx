import React from "react";
import {Meta, Story} from "@storybook/react";

import App from "./App";

export default {
  title: "Placeholder",
  component: App
} as Meta;

export const Default: Story = () => <App />;
